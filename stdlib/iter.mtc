array range(int stop) {
    dynamic array values = [0]
    set values[0] = 0

    int i = 1
    while (i < stop) {
        values.append(i)
        set i = i + 1
    }

    if (stop == 0) {
        dynamic array empty = [0]
        empty.pop()
        return empty
    }

    return values
}
